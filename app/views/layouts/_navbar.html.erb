<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container navbar-container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%#
        Note:

        We need to add both the CSS class 'btn-transform-on-hover' AND the 'ontouchstart="return true;"' attribute to
        the link/logo to make the 3D transform of the button work when tapping it on touch devices (iPhone etc); for
        other (non-touch) devices, the CSS class is enough. Technique taken from:

        http://phonegap-tips.com/articles/essential-phonegap-css-webkit-tap-highlight-color.html
      %>
      <%= link_to root_path, class: "navbar-brand btn-transform-on-hover", ontouchstart: "return true;" do %>
          <%= image_tag(ENV['APP_LOGO'] || "simple_crowd.png", class: "logo") %>
      <% end %>

      <%# hide the slogan on smaller devices because otherwise there is not enough room for the menu options %>
      <div class="nav navbar-nav navbar-left navbar-slogan hidden-sm">
        <%= link_to root_path do %>
            <%= ENV['APP_SLOGAN'] || "simple crowdfunding" %>
        <% end %>
      </div>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <%= render 'layouts/navbar_links' %>
      </ul>
    </div>
  </div>
</nav>
