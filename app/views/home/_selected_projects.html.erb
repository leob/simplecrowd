<%# 'region' is for ARIA see: https://dequeuniversity.com/assets/html/jquery-summit/html5/slides/landmarks.html %>
<div class="content-row-smaller" role="region">

  <div class="row">
    <div class="col-sm-3 col-xs-12">
    </div>

    <div class="col-sm-6 col-xs-12">
      <h3 class="text-center"><%= t('home.selected_projects.title') %></h3>
    </div>

    <div class="col-sm-3 col-xs-12">
      <%#
        Note:

        We need to add both the CSS class 'btn-transform-on-hover' AND the 'ontouchstart="return true;"' attribute to
        the button to make the 3D transform of the button work when tapping it on touch devices (iPhone etc); for other
        (non-touch) devices, the CSS class is enough. Technique taken from:

        http://phonegap-tips.com/articles/essential-phonegap-css-webkit-tap-highlight-color.html
      %>
      <a class="btn btn-success btn-block margin-top btn-transform-on-hover"
         ontouchstart="return true;" href="<%= projects_url %>">
        <%= t('home.selected_projects.more_button') %> &nbsp;<i class="fa fa-arrow-circle-right"></i>
      </a>
    </div>
  </div>

  <%# separator only on XS-screens %>
  <div class="visible-xs-block margin-bottom"></div>

  <%= render 'projects/projects_grid' %>

</div>