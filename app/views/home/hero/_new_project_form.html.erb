<%= form_tag(nil, method: "get", class: "form-inline",
             role: "form") do %>

    <div class="form-group hidden-xs">
      <%= label_tag(:name, t('home.hero_unit.project_title_label'), class: "sr-only") %>
      <%=
          text_field_tag(:name, nil, class: "form-control",
                         placeholder: t('home.hero_unit.project_name_placeholder'),
                         data: {toggle: "tooltip", placement: "bottom", delay: "500", html: "true"},
                         title: t('home.hero_unit.project_name_tooltip_html'))
      %>
    </div>

    <div class="form-group hidden-xs">
      <%= label_tag(:category, t('home.hero_unit.project_category_label'), class: "sr-only") %>
      <%= select_tag(:category,
                     options_from_collection_for_select(@categories, :id, :name),
                     class: "form-control",
                     prompt: t('home.hero_unit.project_category_select'),
                     data: {toggle: "tooltip", placement: "bottom", delay: "500", html: "true"},
                     title: t('home.hero_unit.project_category_tooltip_html')
          ) %>
    </div>

    <%#
        Note:

        We need to add both the CSS class 'btn-transform-on-hover' AND the 'ontouchstart="return true;"' attribute to
        the button to make the 3D transform of the button work when tapping it on touch devices (iPhone etc); for other
        (non-touch) devices, the CSS class is enough. Technique taken from:

        http://phonegap-tips.com/articles/essential-phonegap-css-webkit-tap-highlight-color.html
    %>
    <button class="call-to-action-btn btn-transform-on-hover" type="submit" ontouchstart="return true;"
            data-toggle="tooltip" data-placement="bottom" data-delay="500" data-html="true"
            title="<%= t('home.hero_unit.project_submit_tooltip_html') %>">

        <%= t('home.hero_unit.project_submit') %>&nbsp;&nbsp;&nbsp;<i class="fa fa-arrow-circle-right"></i>
    </button>
<% end %>
